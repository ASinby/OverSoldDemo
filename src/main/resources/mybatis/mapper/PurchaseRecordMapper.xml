<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinby.demo.dao.PurchaseRecordDao">

    <insert id="insertPurchaseRecord" parameterType="purchaesRecord">
        INSERT INTO T_PURCHASE_RECORD(USER_ID, PRODUCT_ID, PRICE, QUANTITY, SUM, NOTE)
        VALUES (#{userId}, #{productId}, #{price}, #{quantity}, #{sum}, #{note})
    </insert>
</mapper>